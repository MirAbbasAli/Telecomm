server:
 port: 9200

spring:
 application:
  name: CallDetailMS
 cloud:
  consul:
   host: localhost
   discovery:
    hostname: localhost
   config:
    fail-fast: false
    enabled: true
    prefixes: config
    defaultContext: application
    profileSeparator: '::'
    format: YAML