server:
 port: 9100

spring:
 application:
  name: CustomerMS
 cloud:
  consul:
   host: localhost
   discovery:
    hostname: localhost
   config:
    fail-fast: false
    enabled: true
    prefixes: config
    defaultContext: application
    profileSeparator: '::'
    format: YAML